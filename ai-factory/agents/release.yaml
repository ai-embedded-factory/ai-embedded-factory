name: release
purpose: 'Prepare releases: versioning, changelog, release notes, audit pack generation.'
inputs:
- repo_state
- task_spec
- constraints
outputs:
- diff
- agent_output.json
rules:
- All artifacts in English
- All CSV use ';' delimiter
- No APPROVED trace row without code_ref + test_id
do_not:
- Do not tag or publish without explicit approval
