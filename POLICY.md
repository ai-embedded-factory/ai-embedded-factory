# Engineering Policy

## Language
All documents and artifacts are in **English**.

## CSV Delimiter
All CSV files MUST use semicolon (`;`).

## Traceability Gate
Any row marked `APPROVED` in `requirements/TRACE_MATRIX.csv` MUST include `code_ref` and `test_id`.
